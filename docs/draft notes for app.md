I need to rebuild the app to work in a different way. We originally had these in the pull-down as options for amino acids and for I Ching and for astrology symbols, but they're not flexible enough.

I need to be able to, as a user, in the app create basic shapes (circles, squares, lines, etc.), but then when it comes to custom shapes, where the app converts the custom shapes to SVGs, those need to be more flexible. I need to be able to upload a file folder, for example SVGs of astrology symbols as PNGs or svgs, convert them to use in the app as mutiple different shapes

but apply the same kind of instance geometry manipulations

depending on how many images or icons are uploaded it needs to ebe able to maniuplate them like it does with the astrology symbols, as instances, in a line or a circle for example, default is  circle like the way the astrology signs work

It needs to adapt to the amount of items uploaded.It needs to adapt to the amount of items uploaded.So for 12 astrological signs, they are uploaded from the folder and spaced out by default around a circle evenly in 12 sectors.

Line molecule.If there are 25 objects uploaded, they are, by default, uploaded into a carousel and displayed with twenty-five sectors

so the app would have basic shapes and symbols and geometries built inallow a user to upload a series of images, symbols, or SVGs to be used as instances.And manipulate them like we are doing with these custom animations.

Anything that isn't a basic shape should be either one image or SVG, and then you can add it to the animation and play with the instances and effects. That's great.

But if I want to upload a series of images or icons, then you need to treat those differently. Those will be treated like we are treating the astrological sign SVGs as a series of images or icons or vectors that will then be mediated.

I need a coherent plan for adding shapes where basic mathematical and polyshapes are part of the pull-down. Shapes like circle, square, vesica.Polyhydron etc that's ok

But for shapes that are not basic mathematical shapes, like the amino acids or anything you may want to upload as a series of objects like astrological signs, they should be uploaded and editable by the user as graphics to be manipulated in this instance player.

Also, we need to add a simple,  PNG, JPEG, or SVG that is just one instance, that manipulated like all other instances if needed.
