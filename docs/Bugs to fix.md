w

the app has a few bugs

* [X] **positioning bug,** sometimes when I add a new shape or statt coring it, it moves to top left corner of canvas, but then resolves after workign with animation a bit

  * [X] this also happens sometimes when apply and removign fills
* [X] SOmetimes when adding a new layer shape, the shape swap out for other shapes in animation, folder shapes for example

  but they seem to resolve over time, or go back to being broken
* [X] Sometimes gradient overlay over an imported SVG shape works, and sometimes it shows as a square  gradient, right now with latest update, it seems to be consistently a square overlay
